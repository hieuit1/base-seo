══════════════════════════════════════
   🔴 KẾT QUẢ CHẤM ĐIỂM SEO
══════════════════════════════════════
   Điểm số:     47/100
   Đánh giá:    KÉM
   Ngưỡng đạt:  60%
   Kết quả:     FAIL
──────────────────────────────────────
   Tổng tiêu chí:  34
   ✅ Đạt:          16
   ❌ Không đạt:    18
══════════════════════════════════════

📋 CHI TIẾT LỖI:
   1. [B2.1 — Thông tin tác giả: Thiếu]
      → Trang thiếu thông tin tác giả (author schema/byline). Google ưu tiên nội dung có tác giả rõ ràng.
   2. [B2.2 — Ngày đăng bài (datePublished): Thiếu]
      → Thiếu datePublished — Google dùng để đánh giá tính mới của nội dung.
   3. [B2.3 — Ngày cập nhật (dateModified): Thiếu]
      → Thiếu dateModified — Bài viết nên hiển thị ngày cập nhật gần nhất.
   4. [B2.4 — Trust Pages: 0/8 trang tồn tại]
      → Chỉ 0 trust pages tồn tại. Cần ≥ 2 trong: About, Contact, Privacy Policy.
   5. [B3.1 — Schema types: [ClothingStore, WebSite, FAQPage] (kỳ vọng: [Organization, BreadcrumbList])]
      → Thiếu Schema types: Organization, BreadcrumbList
   6. [B3.4 — BreadcrumbList Schema: Thiếu]
      → Thiếu BreadcrumbList Schema — giúp Google hiểu cấu trúc site tốt hơn.
   7. [B5.2 — Breadcrumb Schema (JSON-LD): Thiếu]
      → Có breadcrumb nhưng thiếu BreadcrumbList Schema. Thêm JSON-LD để hiển thị trên SERP.
   8. [B6.1 — TTFB: 881ms (tối đa: 800ms)]
      → TTFB quá cao: 881ms, cần ≤ 800ms. Kiểm tra server/hosting/CDN.
   9. [B6.2 — DOM Size: 1711 elements (tối đa: 1500)]
      → DOM quá lớn: 1711 elements, cần ≤ 1500. DOM cồng kềnh làm chậm render.
   10. [B6.5 — Lazy Loading: 32/77 ảnh below-fold (42%)]
      → Chỉ 42% ảnh below-fold có loading="lazy", cần ≥ 80%.
   11. [B6.6 — Font Optimization: font-display:swap=✘, preload=0]
      → Font chưa được tối ưu. Thêm font-display: swap và/hoặc preload font files.
   12. [B6.7 — Responsive Images: 25/113 ảnh có srcset]
      → Chỉ 22% ảnh có srcset/sizes. Nên ≥ 50% để tối ưu cho nhiều device.
   13. [B7.3 — ARIA Labels thiếu: 41 phần tử]
      → 41 phần tử interactive thiếu ARIA label. Ảnh hưởng accessibility.
   14. [B8.1 — WWW Redirect: Lỗi]
      → www ↔ non-www không redirect đúng. Có thể gây duplicate content.
   15. [B8.2 — HTTPS Redirect: Lỗi]
      → HTTP không redirect sang HTTPS. Ảnh hưởng bảo mật và SEO.
   16. [B8.3 — Trailing Slash: Không nhất quán]
      → URL trailing slash không nhất quán. Cần redirect /page và /page/ về cùng 1 phiên bản.
   17. [CrUX — LCP (Largest Contentful Paint): 12245ms]
      → LCP quá chậm (12245ms). Cần ≤ 2500ms.
   18. [CrUX — CLS (Cumulative Layout Shift): 0.123]
      → CLS quá cao (0.123). Cần ≤ 0.1.